import React from 'react';
import PropTypes from 'prop-types';

JoinRouteForm.propTypes = {
    
};

function JoinRouteForm(props) {
    return (
        <div></div>
    );
}

export default JoinRouteForm;